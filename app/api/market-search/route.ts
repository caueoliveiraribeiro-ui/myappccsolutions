import {alphaMarketData,MarketProviderError} from "@/lib/alpha-market-data"
import{NextResponse}from"next/server";import{cookies}from"next/headers";import{verifySession}from"@/lib/auth";
const regionTerms:Record<string,string[]>={US:["United States"],BR:["Brazil"],GB:["United Kingdom"],DE:["Germany"],FR:["France"],ES:["Spain"],IT:["Italy"],PT:["Portugal"],CA:["Canada"],AU:["Australia"],JP:["Japan"],KR:["South Korea","Korea"],MX:["Mexico"],NL:["Netherlands"],CH:["Switzerland"]},searches=new Map<string,{until:number,data:Record<string,unknown>}>();
export async function GET(request:Request){const token=(await cookies()).get("orbit_session")?.value;if(!token||!await verifySession(token))return NextResponse.json({error:"Your session expired. Please sign in again."},{status:401});const u=new URL(request.url),keywords=(u.searchParams.get("keywords")||"").trim(),country=u.searchParams.get("country")||"US",key=process.env.ALPHA_VANTAGE_API_KEY,cacheKey=`${country}:${keywords.toUpperCase()}`,cached=searches.get(cacheKey);if(cached&&cached.until>Date.now())return NextResponse.json(cached.data);if(!keywords)return NextResponse.json({error:"Enter a ticker, company, ETF or fund name."},{status:400});if(!key)return NextResponse.json({error:"Market search is not connected yet. Ask the administrator to check the Alpha Vantage key."},{status:503});try{const d=await alphaMarketData("SYMBOL_SEARCH",keywords);const wanted=regionTerms[country]||[],all=(d.bestMatches||[]).map((x:Record<string,string>)=>({symbol:x["1. symbol"],name:x["2. name"],type:x["3. type"],region:x["4. region"],currency:x["8. currency"],timezone:x["7. timezone"]})),filtered=all.filter((x:Record<string,string>)=>wanted.some(t=>x.region?.toLowerCase().includes(t.toLowerCase()))),result={matches:filtered.slice(0,20),country};searches.set(cacheKey,{until:Date.now()+60*60*1000,data:result});return NextResponse.json(result)}catch(error){if(error instanceof MarketProviderError)return NextResponse.json({error:error.message},{status:error.status,headers:error.retryAfter?{"Retry-After":String(error.retryAfter)}:undefined});return NextResponse.json({error:"We could not reach the market service. Your form is safe; please try again shortly."},{status:502})}}

