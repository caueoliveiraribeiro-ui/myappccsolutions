import{NextResponse}from"next/server";import{cookies}from"next/headers";import{getSession}from"@/lib/auth";import{db}from"@/lib/supabase";export async function POST(request:Request){const token=(await cookies()).get("orbit_session")?.value,user=token?await getSession(token):null;if(!user)return NextResponse.json({error:"Unauthorized"},{status:401});const f=await request.formData(),file=f.get("avatar");if(!(file instanceof File)||!file.type.startsWith("image/")||file.size>2_000_000)return NextResponse.json({error:"Choose an image smaller than 2 MB."},{status:400});const base=process.env.NEXT_PUBLIC_SUPABASE_URL,key=process.env.SUPABASE_SERVICE_ROLE_KEY;if(!base||!key)return NextResponse.json({error:"Storage unavailable"},{status:503});const ext=(file.name.split(".").pop()||"jpg").replace(/[^a-z0-9]/gi,""),path=`${user.id}/profile.${ext}`,r=await fetch(`${base}/storage/v1/object/avatars/${path}`,{method:"POST",headers:{Authorization:`Bearer ${key}`,apikey:key,"Content-Type":file.type,"x-upsert":"true"},body:await file.arrayBuffer()});if(!r.ok)return NextResponse.json({error:await r.text()},{status:502});const avatar_url=`${base}/storage/v1/object/public/avatars/${path}?v=${Date.now()}`;await db("user_profiles?on_conflict=user_id",{method:"POST",headers:{Prefer:"resolution=merge-duplicates,return=representation"},body:JSON.stringify({user_id:user.id,name:user.name||user.email.split("@")[0],email:user.email,avatar_url})});return NextResponse.json({avatar_url})}

