import { NextResponse } from "next/server"
import { checkPassword,createSession,OWNER_ID,verifyUserPassword } from "@/lib/auth"
import { db } from "@/lib/supabase"
const attempts=new Map<string,{count:number;reset:number}>()
export async function POST(request:Request){const ip=request.headers.get("x-forwarded-for")?.split(",")[0]||"local",now=Date.now(),state=attempts.get(ip);if(state&&state.reset>now&&state.count>=5)return NextResponse.json({error:"Too many attempts. Try again in 15 minutes."},{status:429});try{const body=await request.json(),email=String(body.email||"").trim().toLowerCase(),password=String(body.password||"");let id="",valid=false;if(email===process.env.ADMIN_EMAIL?.toLowerCase()&&checkPassword(password)){id=OWNER_ID;valid=true}else{const users=await db(`app_users?email=eq.${encodeURIComponent(email)}&select=id,password_salt,password_hash&limit=1`),user=users?.[0];if(user&&verifyUserPassword(password,user.password_salt,user.password_hash)){id=user.id;valid=true}}if(!valid){attempts.set(ip,{count:(state?.reset&&state.reset>now?state.count:0)+1,reset:now+15*60*1000});return NextResponse.json({error:"Invalid credentials"},{status:401})}attempts.delete(ip);const response=NextResponse.json({ok:true});response.cookies.set("orbit_session",createSession(email,id),{httpOnly:true,secure:process.env.NODE_ENV==="production",sameSite:"strict",path:"/",maxAge:43200});return response}catch{return NextResponse.json({error:"Invalid request"},{status:400})}}

