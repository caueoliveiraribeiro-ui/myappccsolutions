import{NextResponse}from"next/server";import{cookies}from"next/headers";import{getSession}from"@/lib/auth";import{db}from"@/lib/supabase";
export async function DELETE(_:Request,{params}:{params:Promise<{id:string}>}){const token=(await cookies()).get("orbit_session")?.value,user=token?await getSession(token):null;if(!user)return NextResponse.json({error:"Your session expired. Sign in again."},{status:401});const{id}=await params;try{await db(`holdings?portfolio_id=eq.${encodeURIComponent(id)}&user_id=eq.${user.id}`,{method:"PATCH",body:JSON.stringify({portfolio_id:null,updated_at:new Date().toISOString()})});await db(`portfolios?id=eq.${encodeURIComponent(id)}&user_id=eq.${user.id}`,{method:"DELETE"});return NextResponse.json({ok:true})}catch{return NextResponse.json({error:"We could not delete this portfolio. Your assets were not removed."},{status:500})}}

