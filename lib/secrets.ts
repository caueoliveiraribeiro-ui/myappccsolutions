import{createCipheriv,createDecipheriv,createHash,randomBytes}from"node:crypto";const key=()=>createHash("sha256").update(process.env.SESSION_SECRET||"development-only").digest();export function seal(value:string){const iv=randomBytes(12),c=createCipheriv("aes-256-gcm",key(),iv),body=Buffer.concat([c.update(value,"utf8"),c.final()]),tag=c.getAuthTag();return Buffer.concat([iv,tag,body]).toString("base64url")}export function open(value:string){const b=Buffer.from(value,"base64url"),d=createDecipheriv("aes-256-gcm",key(),b.subarray(0,12));d.setAuthTag(b.subarray(12,28));return Buffer.concat([d.update(b.subarray(28)),d.final()]).toString("utf8")}

