"use client"
import { useState } from "react"
import { useRouter } from "next/navigation"
import { Button } from "@/components/ui/button"
import { Input } from "@/components/ui/input"
import { ShieldCheck, Sparkles } from "lucide-react"
export function LoginForm(){const router=useRouter();const[loading,setLoading]=useState(false);const[error,setError]=useState("");async function submit(e:React.FormEvent<HTMLFormElement>){e.preventDefault();setLoading(true);setError("");const f=new FormData(e.currentTarget),r=await fetch("/api/auth/login",{method:"POST",headers:{"content-type":"application/json"},body:JSON.stringify({email:f.get("email"),password:f.get("password")})}),d=await r.json();if(!r.ok){setError(d.error);setLoading(false);return}router.replace("/dashboard");router.refresh()}return <main className="relative grid min-h-screen place-items-center overflow-hidden bg-[#060910] p-5 text-white"><div className="absolute inset-0 bg-[radial-gradient(circle_at_20%_0%,rgba(34,211,238,.28),transparent_34%),radial-gradient(circle_at_80%_100%,rgba(139,92,246,.2),transparent_35%)]"/><div className="relative grid w-full max-w-5xl overflow-hidden rounded-[32px] border border-white/10 bg-white/[.055] shadow-2xl backdrop-blur-2xl lg:grid-cols-[1.15fr_.85fr]"><section className="hidden min-h-[650px] flex-col justify-between border-r border-white/10 p-12 lg:flex"><div className="flex items-center gap-3"><div className="grid h-11 w-11 place-items-center rounded-2xl bg-cyan-300 text-slate-950"><Sparkles/></div><div><div className="font-semibold">Orbit CRM</div><div className="text-xs text-slate-500">Global growth OS</div></div></div><div><div className="mb-5 inline-flex items-center gap-2 rounded-full border border-cyan-300/20 bg-cyan-300/10 px-3 py-1 text-xs text-cyan-200"><ShieldCheck size={14}/>Private business workspace</div><h1 className="max-w-lg text-5xl font-semibold leading-[1.08] tracking-tight">Your leads, creative pipeline and investments. One secure view.</h1><p className="mt-5 max-w-md leading-relaxed text-slate-400">Find global businesses without websites, sell video and web projects, and keep your capital visible.</p></div><p className="text-xs text-slate-600">Encrypted transport · HTTP-only sessions · Server-side verification</p></section><section className="flex min-h-[600px] items-center p-7 sm:p-12"><form onSubmit={submit} className="w-full"><div className="mb-8 lg:hidden"><div className="mb-3 flex items-center gap-2 font-semibold"><Sparkles className="text-cyan-300"/>Orbit CRM</div></div><p className="text-sm text-cyan-300">Welcome back</p><h2 className="mt-2 text-3xl font-semibold">Sign in to your workspace</h2><p className="mt-2 text-sm text-slate-500">Use your private administrator credentials.</p><div className="mt-8 space-y-4"><label className="block text-sm text-slate-300">Email<Input required name="email" type="email" autoComplete="username" className="mt-2 h-12 border-white/10 bg-black/20" placeholder="you@company.com"/></label><label className="block text-sm text-slate-300">Password<Input required name="password" type="password" autoComplete="current-password" className="mt-2 h-12 border-white/10 bg-black/20" placeholder="••••••••••••"/></label>{error&&<div className="rounded-xl border border-red-400/20 bg-red-400/10 p-3 text-sm text-red-200">{error}</div>}<Button disabled={loading} className="h-12 w-full bg-cyan-300 text-slate-950 hover:bg-cyan-200">{loading?"Verifying…":"Secure sign in"}</Button></div><p className="mt-6 text-center text-xs leading-relaxed text-slate-600">Access is rate-limited. Failed attempts are never disclosed in detail.</p></form></section></div></main>}

