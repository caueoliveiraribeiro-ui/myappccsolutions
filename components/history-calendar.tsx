"use client"
import{useEffect,useState}from"react";import{Card}from"@/components/ui/card";import{Button}from"@/components/ui/button";import{Badge}from"@/components/ui/badge";import{CalendarDays}from"lucide-react";import{Area,AreaChart,Bar,BarChart,CartesianGrid,ResponsiveContainer,Tooltip,XAxis,YAxis}from"recharts";import{useCurrencyRates}from"@/components/currency-conversion";type R=Record<string,any>;
const words:R={pt:{Overview:"Visão geral",Clients:"Clientes","Leads Management":"Gestão de leads","Tasks & Follow-ups":"Tarefas e follow-ups",Calendar:"Calendário",Pipeline:"Funil",Projects:"Projetos",Groceries:"Supermercado",Expenses:"Despesas",Stocks:"Ações",Crypto:"Cripto",History:"Histórico",Reports:"Relatórios",Settings:"Configurações","Invite & sharing":"Convites e compartilhamento"},es:{Overview:"Resumen",Clients:"Clientes",Settings:"Configuración"},de:{Overview:"Übersicht",Clients:"Kunden",Settings:"Einstellungen"}};export function translate(text:string,language="en"){return words[language]?.[text]||text}
export function OverviewV2(){return null}
export function CalendarView(){const[data,setData]=useState<R>({events:[]});useEffect(()=>{fetch("/api/calendar/events").then(r=>r.json()).then(setData).catch(()=>{})},[]);return <div className="space-y-4"><Panel title="Google Calendar"><p className="text-sm text-slate-400">Connect your own Google account to see upcoming events.</p>{!data.connected?<Button asChild><a href="/api/calendar/connect"><CalendarDays size={16}/>Connect Google Calendar</a></Button>:<Badge>Google connected</Badge>}</Panel>{data.connected&&(data.events||[]).map((x:R)=><Card key={x.id} className="border-white/10 bg-white/5 p-4 text-white"><p className="text-xs text-cyan-300">{new Date(x.start).toLocaleString()}</p><b>{x.title}</b></Card>)}</div>}
export function HistoryView({expenses,leads,clients,groceries,holdings,portfolios=[],projects,payments=[],language="en",currency="USD"}:R){const marketCurrency=(market:string)=>({BR:"BRL",US:"USD",GB:"GBP",CA:"CAD",AU:"AUD",JP:"JPY",KR:"KRW",MX:"MXN",CH:"CHF",DE:"EUR",FR:"EUR",ES:"EUR",IT:"EUR",PT:"EUR",NL:"EUR"}as R)[market],pricedHoldings=holdings.map((x:R)=>({...x,quote_currency:x.quote_currency||portfolios.find((p:R)=>p.id===x.portfolio_id)?.currency||marketCurrency(x.market)||"USD"})),rows=[...expenses,...groceries,...pricedHoldings,...projects,...leads,...payments],convert=useCurrencyRates(rows,currency),money=(n:any)=>!Number.isFinite(Number(n))?"Conversion unavailable":new Intl.NumberFormat(locale(language),{style:"currency",currency,minimumFractionDigits:2,maximumFractionDigits:2}).format(Number(n||0)),data=monthSeries(12,expenses,leads,projects,pricedHoldings,groceries,convert,money,language,payments);return <div className="space-y-4"><div className="grid gap-4 xl:grid-cols-2"><Chart title="Income and expenses" data={data}/><Panel title="Investments over time"><div className="h-72"><ResponsiveContainer><AreaChart data={data}><CartesianGrid stroke="#ffffff12"/><XAxis dataKey="label"/><YAxis/><Tooltip contentStyle={{background:"#0b1320"}}/><Area dataKey="investments" stroke="#22d3ee" fill="#22d3ee22"/></AreaChart></ResponsiveContainer></div></Panel></div><Panel title={`Monthly details · converted to ${currency}`}><p className="mb-3 text-xs text-slate-500">Every historical purchase is converted from its original purchase currency into your current default currency.</p><div className="space-y-3">{[...data].reverse().map((x:R)=><details key={x.key} className="group rounded-2xl border border-white/10 bg-black/20 open:border-cyan-300/30"><summary className="flex cursor-pointer list-none flex-wrap items-center justify-between gap-4 p-4"><div><b>{x.fullLabel}</b><p className="text-xs text-slate-500">{x.purchases} investment purchases · {x.groceryItems} grocery items · {x.delivered} paid projects</p></div><div className="flex items-center gap-5"><div className="text-right"><p className="text-[10px] uppercase text-slate-500">Invested · {currency}</p><b className="text-cyan-300">{money(x.investments)}</b></div><div className="text-right"><p className="text-[10px] uppercase text-slate-500">Net result</p><b className={x.net>=0?"text-emerald-300":"text-red-300"}>{money(x.net)}</b></div><span className="text-cyan-300 transition group-open:rotate-180">⌄</span></div></summary><div className="border-t border-white/10 p-4"><div className="grid grid-cols-2 gap-2 text-xs md:grid-cols-4"><Stat n="Income" v={money(x.income)}/><Stat n="Expenses" v={money(x.expenses)}/><Stat n="Groceries" v={money(x.groceries)}/><Stat n={`Invested · ${currency}`} v={money(x.investments)}/><Stat n="Investment purchases" v={x.purchases}/><Stat n="New clients" v={x.clients}/><Stat n="Won leads" v={x.won}/><Stat n="Paid projects" v={x.delivered}/></div><div className="mt-4"><p className="mb-2 text-xs font-semibold text-slate-300">Investment purchases</p>{x.assetNames.length?<div className="space-y-2">{x.assetNames.map((asset:string,index:number)=><div key={index} className="rounded-xl border border-cyan-300/10 bg-cyan-300/[.035] p-3 text-xs text-slate-300">{asset}</div>)}</div>:<p className="text-xs text-slate-500">No investment purchases recorded this month.</p>}</div></div></details>)}</div></Panel></div>}
function Chart({title,data}:R){return <Panel title={title}><div className="h-72"><ResponsiveContainer><BarChart data={data}><CartesianGrid stroke="#ffffff12"/><XAxis dataKey="label"/><YAxis/><Tooltip contentStyle={{background:"#0b1320"}}/><Bar dataKey="income" fill="#34d399"/><Bar dataKey="expenses" fill="#fb7185"/></BarChart></ResponsiveContainer></div></Panel>}
export function monthSeries(count:number,expenses:R[],leads:R[],projects:R[],holdings:R[],groceries:R[],convert:(amount:unknown,row:R)=>number=(amount)=>Number(amount||0),money:(amount:unknown)=>string=(amount)=>String(amount||0),language="en",payments?:R[]){const now=new Date();return Array.from({length:count},(_,i)=>{const d=new Date(now.getFullYear(),now.getMonth()-(count-1-i),1),key=`${d.getFullYear()}-${String(d.getMonth()+1).padStart(2,"0")}`,same=(v:any)=>String(v||"").slice(0,7)===key,hs=holdings.filter(x=>same(x.purchased_at||x.created_at)),gs=groceries.filter(x=>same(x.month||x.created_at)),won=leads.filter(x=>["Client","Won"].includes(x.status)&&same(x.updated_at||x.created_at)),done=projects.filter(x=>x.stage==="Payment received"&&same(x.updated_at||x.created_at)),income=payments?payments.filter(x=>same(x.received_at||x.created_at)).reduce((n,x)=>n+convert(x.amount,x),0):done.reduce((n,x)=>n+convert(Number(x.budget||0)-Number(x.cost||0),x),0),expenseTotal=expenses.filter(x=>same(x.expense_date||x.created_at)).reduce((n,x)=>n+convert(x.amount,x),0),groceryTotal=gs.reduce((n,x)=>n+convert(x.actual_cost||x.estimated_cost||0,x),0),investments=hs.reduce((n,x)=>n+convert(Number(x.quantity||0)*Number(x.buy_price||0),x),0),assetNames=hs.map(x=>`${x.asset_type}: ${x.symbol} · ${Number(x.quantity||0)} units @ ${money(convert(x.buy_price,x))} · total ${money(convert(Number(x.quantity||0)*Number(x.buy_price||0),x))}`);return{key,label:d.toLocaleDateString(locale(language),{month:"short"}),fullLabel:d.toLocaleDateString(locale(language),{month:"long",year:"numeric"}),income,expenses:expenseTotal+groceryTotal,groceries:groceryTotal,investments,purchases:hs.length,assetNames,groceryItems:gs.length,clients:leads.filter(x=>same(x.created_at)).length,won:won.length,delivered:done.length,net:income-expenseTotal-groceryTotal}})}
function Stat({n,v}:R){return <div className="rounded-xl bg-white/[.04] p-2"><p className="text-slate-500">{n}</p><b>{v}</b></div>}function Panel({title,children}:R){return <Card className="border-white/10 bg-white/5 p-5 text-white"><h2 className="mb-4 font-semibold">{title}</h2>{children}</Card>}function locale(x:string){return({pt:"pt-BR",es:"es-ES",de:"de-DE",fr:"fr-FR",it:"it-IT",nl:"nl-NL",ja:"ja-JP",ko:"ko-KR"}as R)[x]||"en-US"}
