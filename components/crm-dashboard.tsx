"use client"
import { useEffect, useMemo, useState } from "react"
import { useRouter } from "next/navigation"
import { Button } from "@/components/ui/button"
import { Input } from "@/components/ui/input"
import { Badge } from "@/components/ui/badge"
import { Card } from "@/components/ui/card"
import { Dialog, DialogContent, DialogHeader, DialogTitle, DialogTrigger } from "@/components/ui/dialog"
import { Select, SelectContent, SelectItem, SelectTrigger, SelectValue } from "@/components/ui/select"
import { Textarea } from "@/components/ui/textarea"
import { toast } from "sonner"
import { BarChart3, Bitcoin, BriefcaseBusiness, Building2, ChevronRight, CircleDollarSign, Globe2, LayoutDashboard, LogOut, Menu, Plus, Search, Settings, ShieldCheck, Sparkles, TrendingUp, Users, Video, WalletCards, X } from "lucide-react"
type Lead={id:string;company:string;city:string;country:string;service:"Website"|"Video"|"Both";source:string;status:"New"|"Contacted"|"Qualified";value:number;website?:string}
type Asset={id:string;symbol:string;name:string;type:"Stock"|"Crypto";quantity:number;buyPrice:number;currentPrice:number}
const seedLeads:Lead[]=[{id:"1",company:"Casa Maré Guesthouse",city:"Lisbon",country:"Portugal",service:"Both",source:"Airbnb research",status:"New",value:2400},{id:"2",company:"North Shore Dental",city:"Auckland",country:"New Zealand",service:"Website",source:"Google Maps",status:"Qualified",value:3200},{id:"3",company:"Kumo Ramen Studio",city:"Osaka",country:"Japan",service:"Video",source:"Google Maps",status:"Contacted",value:1800},{id:"4",company:"Nomad Desert Camp",city:"Marrakesh",country:"Morocco",service:"Both",source:"Hospitality research",status:"New",value:4100}]
const seedAssets:Asset[]=[{id:"1",symbol:"NVDA",name:"NVIDIA",type:"Stock",quantity:12,buyPrice:118.4,currentPrice:126.8},{id:"2",symbol:"BTC",name:"Bitcoin",type:"Crypto",quantity:.08,buyPrice:84600,currentPrice:91200},{id:"3",symbol:"ETH",name:"Ethereum",type:"Crypto",quantity:1.6,buyPrice:3180,currentPrice:3460}]
const nav=[["Overview",LayoutDashboard],["Global leads",Globe2],["Video pipeline",Video],["Portfolio",WalletCards],["Clients",Users],["Settings",Settings]] as const
export function CRMDashboard(){const router=useRouter();const[section,setSection]=useState("Overview");const[mobileOpen,setMobileOpen]=useState(false);const[query,setQuery]=useState("");const[leads,setLeads]=useState<Lead[]>(seedLeads);const[assets,setAssets]=useState<Asset[]>(seedAssets);const[leadOpen,setLeadOpen]=useState(false);const[assetOpen,setAssetOpen]=useState(false);const[finding,setFinding]=useState(false);useEffect(()=>{const l=localStorage.getItem("orbit-leads"),a=localStorage.getItem("orbit-assets");if(l)setLeads(JSON.parse(l));if(a)setAssets(JSON.parse(a))},[]);useEffect(()=>{localStorage.setItem("orbit-leads",JSON.stringify(leads))},[leads]);useEffect(()=>{localStorage.setItem("orbit-assets",JSON.stringify(assets))},[assets]);const pipeline=leads.reduce((s,i)=>s+i.value,0),portfolio=assets.reduce((s,i)=>s+i.quantity*i.currentPrice,0),cost=assets.reduce((s,i)=>s+i.quantity*i.buyPrice,0);const filteredLeads=useMemo(()=>leads.filter(l=>`${l.company} ${l.city} ${l.country} ${l.service}`.toLowerCase().includes(query.toLowerCase())),[leads,query]);async function findBusinesses(){if(!query.trim())return toast.error("Enter a city and business category");setFinding(true);try{const r=await fetch(`/api/google-leads?q=${encodeURIComponent(query)}`),d=await r.json();if(!r.ok)throw new Error(d.error);setLeads(c=>[...d.leads,...c.filter((l:Lead)=>!d.leads.some((n:Lead)=>n.id===l.id))]);toast.success(`${d.leads.length} businesses without a website found`)}catch(e){toast.error(e instanceof Error?e.message:"Search failed")}finally{setFinding(false)}}async function logout(){await fetch("/api/auth/logout",{method:"POST"});router.replace("/");router.refresh()}return <main className="min-h-screen bg-[#060910] text-white selection:bg-cyan-300/30"><div className="fixed inset-0 bg-[radial-gradient(circle_at_15%_0%,rgba(22,78,99,.4),transparent_32%),radial-gradient(circle_at_85%_20%,rgba(99,50,150,.24),transparent_35%),linear-gradient(145deg,#060910,#0a1120_55%,#071018)]"/><div className="relative mx-auto flex min-h-screen max-w-[1800px] gap-4 p-3 lg:p-5"><aside className={`${mobileOpen?"flex":"hidden"} fixed inset-3 z-50 w-[280px] flex-col rounded-[28px] border border-white/10 bg-[#09101d]/95 p-4 shadow-2xl backdrop-blur-2xl lg:sticky lg:top-5 lg:flex lg:h-[calc(100vh-40px)] lg:w-[248px] lg:shrink-0`}><div className="mb-8 flex items-center justify-between px-2 pt-2"><div className="flex items-center gap-3"><div className="grid h-10 w-10 place-items-center rounded-2xl bg-gradient-to-br from-cyan-300 to-blue-600 text-slate-950"><Sparkles size={20}/></div><div><div className="font-semibold tracking-tight">Orbit CRM</div><div className="text-xs text-slate-500">Global growth OS</div></div></div><button className="lg:hidden" onClick={()=>setMobileOpen(false)}><X/></button></div><nav className="space-y-1">{nav.map(([label,Icon])=><button key={label} onClick={()=>{setSection(label);setMobileOpen(false)}} className={`group flex w-full items-center gap-3 rounded-2xl px-3 py-2.5 text-sm transition ${section===label?"bg-white/10 text-white":"text-slate-400 hover:bg-white/5 hover:text-white"}`}><Icon size={18} className={section===label?"text-cyan-300":""}/><span>{label}</span>{section===label&&<ChevronRight size={15} className="ml-auto"/>}</button>)}</nav><div className="mt-auto rounded-2xl border border-cyan-300/15 bg-cyan-300/[.06] p-4"><div className="mb-2 flex items-center gap-2 text-sm font-medium"><ShieldCheck size={16} className="text-cyan-300"/>Protected workspace</div><p className="text-xs leading-relaxed text-slate-400">Secure, HTTP-only session. Keep production keys only in Vercel.</p></div><Button variant="ghost" onClick={logout} className="mt-2 justify-start gap-3 text-slate-400 hover:bg-red-400/10 hover:text-red-300"><LogOut size={17}/>Sign out</Button></aside><section className="min-w-0 flex-1"><header className="mb-4 flex items-center justify-between rounded-[24px] border border-white/10 bg-white/[.055] px-4 py-3 backdrop-blur-2xl lg:px-6"><div className="flex items-center gap-3"><button onClick={()=>setMobileOpen(true)} className="lg:hidden"><Menu/></button><div><h1 className="text-lg font-semibold lg:text-xl">{section}</h1><p className="hidden text-xs text-slate-500 sm:block">Build pipeline, close projects, monitor capital.</p></div></div><div className="flex items-center gap-2"><div className="relative hidden sm:block"><Search className="absolute left-3 top-2.5 text-slate-500" size={16}/><Input value={query} onChange={e=>setQuery(e.target.value)} placeholder="Search leads or enter a market…" className="w-72 border-white/10 bg-black/20 pl-9"/></div><div className="grid h-10 w-10 place-items-center rounded-full border border-white/10 bg-gradient-to-br from-cyan-400/30 to-violet-500/30 text-sm font-semibold">CO</div></div></header>{section==="Overview"&&<Overview leads={leads} pipeline={pipeline} portfolio={portfolio} cost={cost} setSection={setSection}/>} {section==="Global leads"&&<LeadsView leads={filteredLeads} query={query} setQuery={setQuery} finding={finding} findBusinesses={findBusinesses} open={leadOpen} setOpen={setLeadOpen} addLead={l=>setLeads(v=>[l,...v])}/>} {section==="Video pipeline"&&<VideoPipeline leads={leads}/>} {section==="Portfolio"&&<Portfolio assets={assets} cost={cost} portfolio={portfolio} open={assetOpen} setOpen={setAssetOpen} addAsset={a=>setAssets(v=>[a,...v])}/>} {section==="Clients"&&<LeadsView leads={filteredLeads.filter(l=>l.status!=="New")} query={query} setQuery={setQuery} finding={finding} findBusinesses={findBusinesses} open={leadOpen} setOpen={setLeadOpen} addLead={l=>setLeads(v=>[l,...v])}/>} {section==="Settings"&&<SettingsView/>}</section></div></main>}
function Metric({title,value,sub,icon:Icon}:{title:string;value:string;sub:string;icon:any}){return <Card className="border-white/10 bg-white/[.055] p-5 text-white backdrop-blur-xl"><div className="mb-5 flex items-start justify-between"><span className="text-sm text-slate-400">{title}</span><div className="rounded-xl bg-white/[.06] p-2 text-cyan-300"><Icon size={18}/></div></div><div className="text-2xl font-semibold tracking-tight">{value}</div><div className="mt-1 text-xs text-emerald-300">{sub}</div></Card>}
function Overview({leads,pipeline,portfolio,cost,setSection}:{leads:Lead[];pipeline:number;portfolio:number;cost:number;setSection:(x:string)=>void}){return <div className="space-y-4"><div className="grid gap-3 sm:grid-cols-2 xl:grid-cols-4"><Metric title="Global leads" value={String(leads.length)} sub={`${leads.filter(l=>l.status==="New").length} ready to contact`} icon={Globe2}/><Metric title="Sales pipeline" value={money(pipeline)} sub="Across video + web" icon={TrendingUp}/><Metric title="Portfolio value" value={money(portfolio)} sub={`${cost?(((portfolio-cost)/cost)*100).toFixed(1):0}% total return`} icon={CircleDollarSign}/><Metric title="Qualified clients" value={String(leads.filter(l=>l.status==="Qualified").length)} sub="High intent" icon={Users}/></div><div className="grid gap-4 xl:grid-cols-[1.5fr_1fr]"><Card className="border-white/10 bg-white/[.055] p-5 text-white"><div className="mb-5 flex items-center justify-between"><div><h2 className="font-semibold">Priority opportunities</h2><p className="text-xs text-slate-500">Ranked by potential deal value</p></div><Button size="sm" variant="ghost" onClick={()=>setSection("Global leads")}>View all</Button></div><div className="space-y-2">{[...leads].sort((a,b)=>b.value-a.value).slice(0,5).map(l=><LeadRow key={l.id} lead={l}/>)}</div></Card><Card className="overflow-hidden border-white/10 bg-gradient-to-br from-cyan-400/10 to-violet-500/10 p-6 text-white"><div className="mb-8 grid h-11 w-11 place-items-center rounded-2xl bg-cyan-300 text-slate-950"><Sparkles/></div><Badge className="mb-3 border-cyan-300/20 bg-cyan-300/10 text-cyan-200">Lead intelligence</Badge><h2 className="text-2xl font-semibold leading-tight">Find businesses that are missing a website.</h2><p className="mt-3 text-sm leading-relaxed text-slate-400">Search by niche and city. Orbit checks Google Places results and keeps opportunities without a listed website.</p><Button onClick={()=>setSection("Global leads")} className="mt-6 bg-cyan-300 text-slate-950 hover:bg-cyan-200">Start prospecting <ChevronRight size={16}/></Button></Card></div></div>}
function LeadRow({lead}:{lead:Lead}){return <div className="grid grid-cols-[1fr_auto] items-center gap-3 rounded-2xl border border-white/[.06] bg-black/15 p-3 sm:grid-cols-[1fr_130px_100px]"><div className="min-w-0"><div className="truncate text-sm font-medium">{lead.company}</div><div className="truncate text-xs text-slate-500">{lead.city}, {lead.country} · {lead.source}</div></div><Badge className="hidden justify-self-start border-white/10 bg-white/[.06] text-slate-300 sm:inline-flex">{lead.service}</Badge><div className="text-right"><div className="text-sm font-medium">{money(lead.value)}</div><div className="text-[11px] text-cyan-300">{lead.status}</div></div></div>}
function LeadsView({leads,query,setQuery,finding,findBusinesses,open,setOpen,addLead}:{leads:Lead[];query:string;setQuery:(x:string)=>void;finding:boolean;findBusinesses:()=>void;open:boolean;setOpen:(x:boolean)=>void;addLead:(l:Lead)=>void}){return <div className="space-y-4"><Card className="border-white/10 bg-white/[.055] p-5 text-white"><div className="flex flex-col gap-4 lg:flex-row lg:items-end lg:justify-between"><div><Badge className="mb-3 border-violet-300/20 bg-violet-300/10 text-violet-200">Google Places lead finder</Badge><h2 className="text-2xl font-semibold">Discover businesses without websites</h2><p className="mt-1 text-sm text-slate-400">Try “wedding photographers in Miami” or “restaurants in Porto”.</p></div><div className="flex flex-col gap-2 sm:flex-row"><Input value={query} onChange={e=>setQuery(e.target.value)} className="min-w-72 border-white/10 bg-black/20" placeholder="Niche + city or country"/><Button disabled={finding} onClick={findBusinesses} className="bg-cyan-300 text-slate-950 hover:bg-cyan-200"><Search size={16}/>{finding?"Checking websites…":"Find leads"}</Button><LeadDialog open={open} setOpen={setOpen} addLead={addLead}/></div></div></Card><Card className="border-white/10 bg-white/[.055] p-4 text-white"><div className="mb-3 flex items-center justify-between px-1"><span className="text-sm text-slate-400">{leads.length} opportunities</span><span className="text-xs text-slate-500">Contact prospects in compliance with local law and platform terms.</span></div><div className="space-y-2">{leads.map(l=><LeadRow key={l.id} lead={l}/>)}</div></Card></div>}
function LeadDialog({open,setOpen,addLead}:{open:boolean;setOpen:(x:boolean)=>void;addLead:(l:Lead)=>void}){return <Dialog open={open} onOpenChange={setOpen}><DialogTrigger asChild><Button variant="outline" className="border-white/10 bg-white/5 text-white"><Plus size={16}/>Add manually</Button></DialogTrigger><DialogContent className="border-white/10 bg-[#0c1422] text-white"><DialogHeader><DialogTitle>New opportunity</DialogTitle></DialogHeader><form className="space-y-3" onSubmit={e=>{e.preventDefault();const f=new FormData(e.currentTarget);addLead({id:crypto.randomUUID(),company:String(f.get("company")),city:String(f.get("city")),country:String(f.get("country")),service:String(f.get("service")) as Lead["service"],source:"Manual",status:"New",value:Number(f.get("value"))});setOpen(false);toast.success("Lead added")}}><Input name="company" required placeholder="Business name"/><div className="grid grid-cols-2 gap-3"><Input name="city" required placeholder="City"/><Input name="country" required placeholder="Country"/></div><Select name="service" defaultValue="Both"><SelectTrigger><SelectValue/></SelectTrigger><SelectContent><SelectItem value="Website">Website</SelectItem><SelectItem value="Video">Video</SelectItem><SelectItem value="Both">Video + website</SelectItem></SelectContent></Select><Input name="value" type="number" min="0" required placeholder="Estimated value (USD)"/><Button className="w-full bg-cyan-300 text-slate-950">Save opportunity</Button></form></DialogContent></Dialog>}
function VideoPipeline({leads}:{leads:Lead[]}){const cols=["New","Contacted","Qualified"] as const;return <div className="grid gap-4 xl:grid-cols-3">{cols.map((status,i)=><Card key={status} className="min-h-[520px] border-white/10 bg-white/[.045] p-4 text-white"><div className="mb-4 flex items-center justify-between"><div className="flex items-center gap-2"><span className={`h-2.5 w-2.5 rounded-full ${i===0?"bg-cyan-300":i===1?"bg-violet-300":"bg-emerald-300"}`}/><h2 className="font-medium">{status}</h2></div><Badge className="bg-white/5 text-slate-400">{leads.filter(l=>l.status===status).length}</Badge></div><div className="space-y-3">{leads.filter(l=>l.status===status).map(l=><div key={l.id} className="rounded-2xl border border-white/[.07] bg-black/20 p-4"><div className="mb-1 font-medium">{l.company}</div><div className="text-xs text-slate-500">{l.city}, {l.country}</div><div className="my-4 flex gap-2"><Badge className="border-white/10 bg-white/5 text-slate-300">{l.service}</Badge><Badge className="border-white/10 bg-white/5 text-slate-300">{l.source}</Badge></div><div className="flex items-center justify-between"><span className="text-xs text-slate-500">Potential</span><span className="text-sm font-semibold">{money(l.value)}</span></div></div>)}</div></Card>)}</div>}
function Portfolio({assets,cost,portfolio,open,setOpen,addAsset}:{assets:Asset[];cost:number;portfolio:number;open:boolean;setOpen:(x:boolean)=>void;addAsset:(a:Asset)=>void}){return <div className="space-y-4"><div className="grid gap-3 sm:grid-cols-3"><Metric title="Market value" value={money(portfolio)} sub="Manual or API refreshed prices" icon={WalletCards}/><Metric title="Invested capital" value={money(cost)} sub="Weighted purchase basis" icon={BriefcaseBusiness}/><Metric title="Unrealized return" value={money(portfolio-cost)} sub={`${cost?(((portfolio-cost)/cost)*100).toFixed(2):0}% gain/loss`} icon={BarChart3}/></div><Card className="border-white/10 bg-white/[.055] p-5 text-white"><div className="mb-5 flex items-center justify-between"><div><h2 className="font-semibold">Stocks & crypto</h2><p className="text-xs text-slate-500">Track purchases and performance in one view.</p></div><AssetDialog open={open} setOpen={setOpen} addAsset={addAsset}/></div><div className="space-y-2">{assets.map(a=>{const gain=(a.currentPrice-a.buyPrice)*a.quantity;return <div key={a.id} className="grid grid-cols-[1fr_auto] gap-3 rounded-2xl border border-white/[.06] bg-black/15 p-4 sm:grid-cols-[1.4fr_repeat(4,1fr)]"><div className="flex items-center gap-3"><div className="grid h-9 w-9 place-items-center rounded-xl bg-white/[.07]">{a.type==="Crypto"?<Bitcoin size={18} className="text-amber-300"/>:<TrendingUp size={18} className="text-cyan-300"/>}</div><div><div className="text-sm font-semibold">{a.symbol}</div><div className="text-xs text-slate-500">{a.name}</div></div></div><Data label="Quantity" value={String(a.quantity)}/><Data label="Average buy" value={money(a.buyPrice)}/><Data label="Market value" value={money(a.currentPrice*a.quantity)}/><Data label="Return" value={`${gain>=0?"+":""}${money(gain)}`} green={gain>=0}/></div>})}</div></Card><p className="text-xs text-slate-500">Portfolio data is for tracking only and is not financial advice.</p></div>}
function Data({label,value,green}:{label:string;value:string;green?:boolean}){return <div className="hidden sm:block"><div className="text-[11px] text-slate-500">{label}</div><div className={`mt-1 text-sm ${label==="Return"?(green?"text-emerald-300":"text-red-300"):""}`}>{value}</div></div>}
function AssetDialog({open,setOpen,addAsset}:{open:boolean;setOpen:(x:boolean)=>void;addAsset:(a:Asset)=>void}){return <Dialog open={open} onOpenChange={setOpen}><DialogTrigger asChild><Button className="bg-cyan-300 text-slate-950"><Plus size={16}/>Record purchase</Button></DialogTrigger><DialogContent className="border-white/10 bg-[#0c1422] text-white"><DialogHeader><DialogTitle>Record an investment</DialogTitle></DialogHeader><form className="space-y-3" onSubmit={e=>{e.preventDefault();const f=new FormData(e.currentTarget);addAsset({id:crypto.randomUUID(),symbol:String(f.get("symbol")).toUpperCase(),name:String(f.get("name")),type:String(f.get("type")) as Asset["type"],quantity:Number(f.get("quantity")),buyPrice:Number(f.get("buyPrice")),currentPrice:Number(f.get("currentPrice"))});setOpen(false);toast.success("Purchase recorded")}}><div className="grid grid-cols-2 gap-3"><Input required name="symbol" placeholder="Ticker"/><Input required name="name" placeholder="Asset name"/></div><Select name="type" defaultValue="Stock"><SelectTrigger><SelectValue/></SelectTrigger><SelectContent><SelectItem value="Stock">Stock</SelectItem><SelectItem value="Crypto">Crypto</SelectItem></SelectContent></Select><div className="grid grid-cols-3 gap-3"><Input required name="quantity" type="number" step="any" placeholder="Quantity"/><Input required name="buyPrice" type="number" step="any" placeholder="Buy price"/><Input required name="currentPrice" type="number" step="any" placeholder="Current price"/></div><Button className="w-full bg-cyan-300 text-slate-950">Save purchase</Button></form></DialogContent></Dialog>}
function SettingsView(){return <div className="grid gap-4 lg:grid-cols-2"><Card className="border-white/10 bg-white/[.055] p-6 text-white"><ShieldCheck className="mb-4 text-cyan-300"/><h2 className="text-lg font-semibold">Security</h2><p className="mt-2 text-sm leading-relaxed text-slate-400">Sessions are signed server-side, stored in HTTP-only cookies, use SameSite strict and secure mode in production. Configure a long SESSION_SECRET and a scrypt password hash in Vercel.</p></Card><Card className="border-white/10 bg-white/[.055] p-6 text-white"><Building2 className="mb-4 text-violet-300"/><h2 className="text-lg font-semibold">Integrations</h2><p className="mt-2 text-sm leading-relaxed text-slate-400">Add GOOGLE_PLACES_API_KEY in Vercel to enable worldwide business discovery. Airbnb prospecting must use authorized/public data and respect local privacy and platform terms.</p></Card><Card className="border-white/10 bg-white/[.055] p-6 text-white lg:col-span-2"><h2 className="text-lg font-semibold">Outreach templates</h2><Textarea className="mt-4 min-h-32 border-white/10 bg-black/20" defaultValue="Hi {{name}}, I noticed {{business}} has a strong local presence but no dedicated website. We help hospitality and service businesses turn more searches into bookings with conversion-focused websites and short-form video. Would a 10-minute audit be useful?"/></Card></div>}
function money(value:number){return new Intl.NumberFormat("en-US",{style:"currency",currency:"USD",maximumFractionDigits:0}).format(value)}

